import express from "express";
import UserController from "../controllers/UserController";

const router = express.Router();

// router.post("/sign-up-email", UserController.signUpEmailAndPassword);
// router.post("/sign-in-email", UserController.signInWithEmailAndPassword);
// router.post("/is-verified", UserController.verifyToken ,UserController.isVerified);
// // get email
// router.post("/get-email", UserController.getEmailFromToken);
// router.post("/verify-token", UserController.verifyToken);

// // verification code
// router.post("/create-verification-code", UserController.createVerificationCode);
// router.post("/get-verification-code", UserController.getVerificationCode);
// router.post("/verify-verification-code", UserController.verifyVerificationCode);

export default router;
